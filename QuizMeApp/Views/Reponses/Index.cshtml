@model IEnumerable<QuizMeApp.Models.Reponse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Apprenant.nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Question.enonce)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.choix)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateReponse)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Apprenant.nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Question.enonce)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.choix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateReponse)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
